{% extends "base.html" %}
{% load i18n %}
{% load fb_versions %}



{% block allcontent %}
  {% if user.is_authenticated %}
{% block pagetitle %}
	<legend> Initited Campaigns </legend>
{% endblock %}
    {% if campaigns %}
		{% for camp in campaigns %}
		<ul class="list-group">
		  <a href= "{{camp.pk}}" >
			  <li class="list-group-item">
				<span class="badge">Prio: {{ camp.prio }}</span>
				<strong><span class="label label-primary">Until: {{ camp.when }}</span> -- {{camp.prio}}: {{camp}} | </strong>{{ camp.description|truncatewords:10 }} ... 
			  </li>
		  </a>
		</ul>
		{% endfor  %}
    {% else %}
        <p> no initiate campaigns </p>
    {% endif %}
  {% endif %}

	<legend> Public Campaigns </legend>
    {% if campaigns_public %}
		{% for camp in campaigns_public %}
		<ul class="list-group">
		  <a href= "{{camp.pk}}" >
			  <li class="list-group-item">
				<span class="badge">Prio: {{ camp.prio }}</span>
				<strong><span class="label label-primary">Until: {{ camp.when }}</span> -- {{camp.prio}}: {{camp}} | </strong>{{ camp.description|truncatewords:10 }} ... 
			  </li>
		  </a>
		</ul>
		{% endfor  %}
    {% else %}
        <p> no public campaigns </p>
    {% endif %}
	
  {% if user.is_authenticated %}  
	<legend> <small> Campaigns Archive </small> </legend>

    {% if campaigns_archive %}
		{% for camp in campaigns_expired %}
		<ul class="list-group">
		  <a href= "{{camp.pk}}" >
			  <li class="list-group-item">
				<span class="badge">Prio: {{ camp.prio }}</span>
				<strong><span class="label label-primary">Until: {{ camp.when }}</span> -- {{camp.prio}}: {{camp}} | </strong>{{ camp.description|truncatewords:10 }} ... 
			  </li>
		  </a>
		</ul>
		{% endfor  %}
    {% else %}
        <p> no archived campaigns </p>
    {% endif %}
  {% endif %}
{% endblock %}
