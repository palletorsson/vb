{% extends "base.html" %}
{% load i18n %}
{% load fb_versions %}



{% block pagetitle %}
	<legend> <a href="/projects/">Projects</a> /  {{project}}  ({{ project.prio }} {{ project.active }})</legend>
{% endblock %}

{% block allcontent %}
<div class="container">
<div class="row">
  <div class="col-xs-4 col-sm-4">
  {% if user.is_authenticated %}
	<h4>Description</h4>
    <hr> 
	 <ul>
	   <li class="list-group-item">
			{{project.description}}
	   </il>
	 </ul>
  </div>
    <h4>Texts</h4>
       <hr>
       {% for f in files %}
			<li class="list-group-item"><a href="{{ f.link }}"> <span class="glyphicon glyphicon-file" aria-hidden="true"></span> {{ f.title }}  </a></li>  
	  	{% endfor %}
	   </ul>
  </div>
	{% if media %}
	<div class="col-xs-8 col-sm-8">
		<h4>Media</h4>
		   <hr>
		   {% for m in media %}
		   <span> <h5> <span class="glyphicon glyphicon-picture" aria-hidden="true"></span> :  {{ m.title }} </h5> <a href="{{m.link}}"><img src="{{ f.link }}" class="img-responsive" alt="Responsive image"> </a>       
		   {% endfor %}  
	  </div>
	{% endif %}  
    <div class="col-xs-12 col-sm-11">
	<h4 id="toggle_todo">Do To</h4>
       <hr>
      {% for k in kanban %}
	<div class="" id="todo"> 
		<iframe src="{{k.link}}" width="100%" height="600px">
	</div>
	{% endfor %}
  </div>

{% else %}
<h4>loggin to see Synopsis</h4>
{% endif %}

</div>
</div>

<br>
<br>
<br>
<br>


{% endblock %}

